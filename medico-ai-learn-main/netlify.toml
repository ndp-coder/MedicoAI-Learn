[build]
  command = "rm -rf node_modules && npm install --legacy-peer-deps && npm rebuild lightningcss --update-binary && npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
