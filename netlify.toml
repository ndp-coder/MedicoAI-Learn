[build]
  command = "rm -rf node_modules package-lock.json bun.lockb bun.lock && npm install --legacy-peer-deps && npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200